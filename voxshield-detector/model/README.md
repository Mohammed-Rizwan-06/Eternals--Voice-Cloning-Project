Run from the project root:

git clone https://github.com/clovaai/aasist.git model/aasist

Do not add arbitrary pickle/joblib files from the old scaffold. The project intentionally uses the official AASIST source and its published checkpoint.
